# Environment Variables for Almacena Dashboard
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git!

# ==============================================================================
# GOOGLE CLOUD CREDENTIALS
# ==============================================================================
# Path to your Google Service Account JSON credentials file
# This file should be placed in the credentials/ directory and NEVER committed
GOOGLE_CREDENTIALS_FILE=credentials/your-service-account.json

# Google Drive file ID for the KPI data source
# Extract from the URL: https://drive.google.com/file/d/FILE_ID_HERE/view
GOOGLE_DRIVE_FILE_ID=your_drive_file_id_here

# Google Sheet name to read from (default: dashboard)
GOOGLE_SHEET_NAME=dashboard

# ==============================================================================
# N8N AUTOMATION CREDENTIALS
# ==============================================================================
# N8N web interface credentials
# IMPORTANT: Change these from defaults!
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your_secure_password_here

# N8N configuration
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# Timezone for N8N scheduling (adjust to your timezone)
GENERIC_TIMEZONE=Europe/Madrid

# ==============================================================================
# DASHBOARD SETTINGS
# ==============================================================================
# Data filtering settings (comma-separated list to exclude)
EXCLUDE_PERIODS=Oct-25,Nov-25,Dec-25

# Include periods through (format: Mon-YY)
INCLUDE_PERIODS_THROUGH=Sep-25

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# 1. NEVER commit .env file to git (already in .gitignore)
# 2. Generate strong passwords for N8N (use: python -c "import secrets; print(secrets.token_urlsafe(32))")
# 3. Rotate credentials periodically
# 4. Use minimal permissions on Google service account (read-only access)
# 5. Store credentials securely (use a password manager)

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. Place Google credentials JSON in credentials/ directory
# 4. Update .env with the credentials filename
# 5. Never share or commit the .env file
